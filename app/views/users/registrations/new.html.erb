<h1>新規登録</h1>
<%= link_to 'ログイン画面へ', new_user_session_path, class: 'btn btn-default' %>
<hr class="line2">
<div class="row">
  <div class="col-md-3 new-user-form">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <div class="field row">
        <div class="col-md-12"><%= f.text_field :name, autofocus: true, class: 'form-control', placeholder: '名前', required: true %></div>
        <div class="col-md-12"><%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'email', required: true %></div>
        <div class="col-md-12"><%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'パスワード(6文字以上)', required: true %></div>
        <div class="col-md-12"><%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'パスワード(確認)', required: true %></div>
        <div class="col-md-4"><%= f.label :image %></div>
        <div class="col-md-12"><%= f.file_field :image %></div>
      </div>
      <div class="actions">
        <%= f.submit "登録", class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
